/** @type {import('next').NextConfig} */
const nextConfig = {
  // ... other configurations

  images: {
    domains: ['energyandhumanity.com'],
  },

  // ... other configurations
}

module.exports = nextConfig
